# Parity baselines

- `thresholds.json`: acceptance gates for parity reports produced by `parity_harness.py`.
- `latest_parity_metrics.json`: optional local artifact from a recent run (not required in git).

Use `parity_gate.py` to enforce thresholds in CI.
